---
agent: speckit.engloop.21-postmortem
description: "Start, continue, complete, or analyze an incident postmortem from plain-language context, then produce validated rule/scenario-bound repairs."
argument-hint: "Describe the incident and desired postmortem action; internal IDs and PM path are optional."
---

Collect and confirm the incident plus next registry-backed PM path conversationally when exact bindings are absent; never ask the operator to reconstruct internal flags. Then read current `NORTHSTAR.md`, the relevant `LEARNINGS.md → card → PMxxx/LEARNxxx` path, and—when applicable—the installed native .NET 10 SEK v0.1.3 model/Cord/generated evidence. Classify why testing missed the incident and create Rule-ID/scenario/executable-gate repair contracts only after trusted binding.
